# Render Blueprint for Smriti AI Backend
# Docs: https://render.com/docs/blueprint-spec

services:
  - type: web
    name: smriti-ai-backend
    dockerfilePath: ./Dockerfile
    healthCheckPath: /
    envVars:

